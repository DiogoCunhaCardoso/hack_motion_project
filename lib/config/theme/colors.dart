// I know in large scale projects I should extend material theme and change what I want (this way I can access through context).
// But since this is a simple project I will just create a class with the colors used.

import 'dart:ui';

class AppColors {
  static const Color red = Color(0xFFDB0707);
  static const Color orange = Color(0xFFFF9300);
  static const Color teal = Color(0xFF00C2BC);

  static const Color darkOrange = Color(0xFFCC7500);
  static const Color darkTeal = Color(0xFF007876);
}
